"
I represent an OpenGraph structured property.

Some properties can have extra metadata attached to them. These are specified in the same way as other metadata with property and content, but the property will have extra colon ̀:̀, e.g.  ̀image:width̀.
"
Class {
	#name : #WAOpenGraphProperty,
	#superclass : #WAOpenGraphElement,
	#category : #'Seaside-OpenGraph-Core'
}
